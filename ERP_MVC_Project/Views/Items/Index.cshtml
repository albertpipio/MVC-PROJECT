@model IEnumerable<Item>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Item List</h2>
		</div>
        <div class="col-6 text-end">
            <a asp-controller="Items" asp-action="Create" class="btn btn-success">
               <i class="bi bi-plus-circle"></i> &nbsp; Create New Item
			</a>
		</div>
	</div>
    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Id
			</th>
            <th>
                Description
			</th>
            <th>
                Category
			</th>
            <th>
                Date of Creation
			</th>
            <th>
                
            </th>
		</tr>
	</thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">
                    @obj.Id
				</td>
                <td width="30%">
                    @obj.Description
				</td>
                <td width="30%">
                    @obj.Category
				</td>
                <td width="30%">
                    @obj.CreatedDateTime
				</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Items" asp-action="Edit" asp-route-id="@obj.Id"
                        class="btn btn-warning mx-2"> <i class="bi bi-pencil-square"></i></a>
                        <a asp-controller="Items" asp-action="Delete" asp-route-id="@obj.Id"
                        class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i></a>
					</div>
				</td>
			</tr>
        }
    </tbody>
</table>
